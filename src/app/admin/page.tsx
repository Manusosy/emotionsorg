import AdminPanel from '@/features/admin/pages/AdminPanel';

export const metadata = {
  title: 'Admin Panel | Emotions',
  description: 'Administrative tools and settings for the Emotions application',
};

export default function AdminPage() {
  return <AdminPanel />;
} 